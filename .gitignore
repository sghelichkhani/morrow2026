# Run outputs (regenerate from HPC)
*.out
*.err
*.pbs
*.pvd
*.vtu
*.pvtu
profile_*.txt

# Curated campaign archives contain the raw evidence for published results.
!parallel_scaling/results/profile_campaigns/
!parallel_scaling/results/profile_campaigns/**

# Mesh checkpoints (large binaries kept local; recipe: lower_murrumbidgee_mesh.py)
*.h5

# Regenerated figures — source of truth is the parsed JSON in parsed/.
# Rebuild with `python3 plot_results.py --parsedir parsed --outdir figures`.
parallel_scaling/figures/

# Python / editor junk
__pycache__/
*.pyc
.DS_Store
